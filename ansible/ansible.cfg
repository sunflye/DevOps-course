[defaults]
inventory = inventory/hosts.ini
roles_path = roles
host_key_checking = False
remote_user = ubuntu
retry_files_enabled = False
private_key_file = /workspace/labsuser.pem

[privilege_escalation]
become = True
become_method = sudo
become_user = root